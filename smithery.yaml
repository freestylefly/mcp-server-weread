# Smithery configuration file: https://smithery.ai/docs/config#smitheryyaml

startCommand:
  type: stdio
  configSchema:
    # JSON Schema defining the configuration options for the MCP.
    type: object
    properties: {}
    description: No configuration is required. The MCP server uses the environment
      variable WEREAD_COOKIE.
  commandFunction:
    # A JS function that produces the CLI command based on the given config to start the MCP on stdio.
    |-
    (config) => ({ command: 'node', args: ['build/index.js'], env: { WEREAD_COOKIE: process.env.WEREAD_COOKIE || '' } })
  exampleConfig: {}
